{
  "name": "amazon-sg-price-tracker",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && mkdir -p public && cp ../data/products.json public/products.json && npm run build && cp ../data/products.json dist/products.json"
  }
}