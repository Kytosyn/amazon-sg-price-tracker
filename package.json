{
  "name": "amazon-sg-price-tracker",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p frontend/public && cp -f data/products.json frontend/public/products.json 2>/dev/null; cd frontend && npm install && npm run build"
  }
}
