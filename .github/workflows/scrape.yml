name: DiskPrices SG Scrape

on:
  schedule:
    - cron: '*/30 * * * *'  # Every 30 minutes
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: pip install requests beautifulsoup4 lxml
      
      - name: Run scraper
        id: scrape
        continue-on-error: true
        run: |
          echo "Starting scraper..."
          python scraper.py 2>&1 | tee scraper_output.log
          echo "exit_code=$?" >> $GITHUB_OUTPUT
      
      - name: Show scraper output
        if: always()
        run: cat scraper_output.log || echo "No log"
      
      - name: Export to JSON
        if: steps.scrape.outputs.exit_code == '0'
        run: python export_json.py
      
      - name: Commit and push changes
        if: steps.scrape.outputs.exit_code == '0'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update price data: $(date +%Y-%m-%d-%H%M)"
          file_pattern: "diskprices.db data/products.json"
          commit_user_name: "github-actions"
          commit_user_email: "actions@github.com"
          push_options: --force
          token: ${{ secrets.PAT }}
      
      - name: Upload logs as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-logs
          path: scraper_output.log
          retention-days: 7
