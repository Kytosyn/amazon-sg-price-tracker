{
  "version": 2,
  "buildCommand": "cd frontend && npm install && mkdir -p public && cp ../data/products.json public/products.json 2>/dev/null || true && npm run build && cp ../data/products.json dist/products.json 2>/dev/null || true",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
